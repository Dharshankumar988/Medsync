# ============================================================================
# MedSync Backend — Development Requirements (ARM64 / Windows)
# ============================================================================
# This file installs everything EXCEPT the blockchain native dependencies
# (web3, eth-account, cryptography) that fail to compile on Windows ARM.
#
# Usage:
#   python -m venv venv_dev
#   venv_dev\Scripts\activate
#   pip install -r requirements-dev.txt
#
# Then set in your .env:
#   BLOCKCHAIN_MODE=mock
#
# The backend will start normally using the mock blockchain gateway.
# ============================================================================

# fastapi==0.111.0  (Installed manually with --no-deps to avoid httptools)
uvicorn==0.30.1
sqlalchemy==2.0.30
alembic==1.13.1
pydantic==2.7.4
pydantic-settings==2.3.4
passlib==1.7.4
python-jose==3.3.0
httpx==0.27.0
pytest==8.2.2
pytest-asyncio==0.23.7
email-validator==2.1.1
python-multipart==0.0.9
groq==0.8.0
apscheduler==3.10.4

# --- Omitted (require native compilation on ARM64) ---
# web3==6.19.0
# eth-account==0.11.2
# bcrypt==4.1.3                  (passlib fallback is sufficient for dev)
# python-jose[cryptography]      (using python-jose without cryptography extra)
slowapi==0.1.9
